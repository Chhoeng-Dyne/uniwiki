import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(
    0xFFF9F7F7,
  ); // lightest - page background
  static const Color secondary = Color(
    0xFFDBE2EF,
  ); // light blue-gray - cards, borders
  static const Color primary = Color(
    0xFF3F72AF,
  ); // medium blue - buttons, links, highlights
  static const Color dark = Color(
    0xFF112D4E,
  ); // dark navy - headings, text, app bar
}
